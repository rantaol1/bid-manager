import { PageShell } from '@/components/layout/page-shell'

export default function DashboardPage() {
  return (
    <PageShell title="Dashboard" description="Pipeline overview and recent activity.">
      <p className="text-sm text-muted-foreground">Dashboard coming up in a later build step.</p>
    </PageShell>
  )
}
