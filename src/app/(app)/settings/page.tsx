import { PageShell } from '@/components/layout/page-shell'

export default function SettingsPage() {
  return (
    <PageShell title="Settings" description="Rate card and workspace configuration.">
      <p className="text-sm text-muted-foreground">Settings coming up in a later build step.</p>
    </PageShell>
  )
}
